package us100;

public class TestCase002 {

    // step 1 is done
    // step 2 is done
    // step 3 is done
    // step 4 is done
    // step 5 is done
    // step 6 is done

    //git rm --cached git_practice2.iml// to remove keeping track of folders ".iml"
    //git rm -r --cached .idea// to remove keeping track of folders ".idea"
    //git rm -r --cached out // to remove keeping track of folders "out"

}
